# Server Configuration
# 选择使用的服务端实现: go 或 nodejs
SERVER_TYPE=nodejs

# Go Server 配置
# 如果 SERVER_TYPE=go，以下配置会被使用
GO_SERVER_PORT=3000
GO_SERVER_PATH=./server-go

# Node.js Server 配置
# 如果 SERVER_TYPE=nodejs，以下配置会被使用
NODEJS_SERVER_PORT=3000
NODEJS_SERVER_PATH=./server-nodejs

# Database Configuration
# 数据库配置（用于 install.sh 自动配置）
DB_USER=lothar
DB_NAME=workflow_engine
